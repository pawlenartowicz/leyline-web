# example-vaults/documentation

Demo content for the bundled `documentation` theme. Mounted at
`/documentation` by `config/config.yaml`.

This vault contains the full Leyline manual — concepts, collaboration,
vault and server management, troubleshooting, and reference — as demo
content for the `documentation` theme. Tag the vault's git repo to
populate the version switcher.
